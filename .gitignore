# Optional local secrets (Firebase web config is public-by-design;
# dashboard password lives in Firebase Auth, not in this repo)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
*.log

# Dependencies (if added later)
node_modules/
